<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.offcn.mapper.CityMapper">

    <select id="getCityAll"  resultType="city" parameterType="int">
        SELECT * FROM t_city where c_id=#{id}
    </select>

</mapper>
